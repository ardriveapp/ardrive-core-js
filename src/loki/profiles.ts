export class ProfilesDao {
	//Proposed functions, but any queries from these can be accessed from the collection object
	// getDriveLastBlockHeight() {}
	// getUser() {}
	// getUserWalletBalance() {}
}
